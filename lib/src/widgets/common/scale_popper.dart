import 'package:flutter/widgets.dart';

class ScalePopper extends StatefulWidget {
  const ScalePopper(this.scale, {super.key});

  final double scale;
  
  @override
  State<ScalePopper> createState() => _ScalePopperState();
}

class _ScalePopperState extends State<ScalePopper> {
  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
